// SSR: Read required search parameters natively and ensure it's not cached
export const dynamic = 'force-dynamic';

export default function Success({ searchParams }) {
  return (
    <div>
      {/* Success Content */}
    </div>
  );
}
