// SSR: Always dynamic to fetch latest failure metadata from specific session URLs
export const dynamic = 'force-dynamic';

export default function Failed({ searchParams }) {
  return (
    <div>
      {/* Failed Content */}
    </div>
  );
}
